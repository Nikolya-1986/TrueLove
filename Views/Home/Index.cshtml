@model Love.ViewModels.HomeViewModel

@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">
        @ViewData["Title"]
    </h1>
    @{
        foreach(MainUserInfo mainUserInfo in Model.mainUserInfo) {
            @Html.Partial("MainUserInfo", mainUserInfo)
        }
    }
</div>
